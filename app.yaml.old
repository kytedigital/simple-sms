runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  APP_ENV: production
  APP_LOG: errorlog
  APP_DEBUG: true
  APP_KEY: base64:q0fxs6z6ngsUSOXA19yPedtTRkpTchoQ3k4vshmF0vI=
  APP_URL: https://emperor.appspot.com
  CACHE_DRIVER: file
  SESSION_DRIVER: database
  DB_HOST: localhost
  DB_CONNECTION: mysql
  DB_DATABASE: app_service
  DB_USERNAME: app_service
  DB_PASSWORD: "@1withPenguins"
  DB_SOCKET: /cloudsql/emperor:australia-southeast1:emperor-db-au-1
  MYSQL_DSN: mysql:dbname=app_service;unix_socket=cloudsql/emperor:australia-southeast1:emperor-db-au-1
  BURST_SMS_API_KEY: 230e4ea0fd45e3cba4c93d5bf3d9c296
  BURST_SMS_API_SECRET: apisecretkey
  BURST_SMS_API_BASE: https://api.transmitsms.com/
  BURST_SMS_FIXED_NUMBER: 61428974807
  STORAGE_DIR: /tmp
  MESSAGING_CHANNELS: sms
  APP_TIMEZONE: Australia/Melbourne

beta_settings:
  # for Cloud SQL, set this value to the Cloud SQL connection name,
  # e.g. "project:region:cloudsql-instance"
  cloud_sql_instances: "emperor:australia-southeast1:emperor-db-au-1"